{
  "frontend/src/components/LoginSignup.tsx": "Login/signup component with a 4-step flow (auth → mobile → otp → name); branding text updated from \"WINZONE\" to \"YONO DAN\" in the header section.",
  "frontend/src/pages/GamePlay.tsx": "Full game play page accepting `gameName`, `user`, `onBack`, and `onBalanceUpdate` props (prop-based navigation, no router hooks). Renders game image, bet selector, choice selector, result display, play button with loading state, and game rules; uses local balance state synced from user prop with non-blocking best-effort backend recording.",
  "frontend/src/components/GameGrid.tsx": "Game grid component accepting a `navigateTo` prop, displaying all 6 games (Dragon vs Tiger, 7 Up Down, Andar Bahar, Ludo, Mines, Crash) in two sections with images, badges, player counts, and Play Now buttons that call `navigateTo('game', gameName)` using the full game name as the identifier.",
  "frontend/src/App.tsx": "Main app component with routing extended to include the new 'withdrawal' page type, rendering the Withdrawal component and passing navigateTo to Account page.",
  "frontend/index.html": "HTML entry point with comprehensive SEO meta tags including title \"YONO DAN - Online Gaming Platform\", description, keywords, Open Graph tags, Twitter Card tags, canonical link, and robots meta tag for search engine discoverability.",
  "frontend/public/robots.txt": "Robots.txt file allowing all search engine crawlers to index the YONO DAN site and referencing the sitemap URL.",
  "frontend/public/sitemap.xml": "XML sitemap with the root URL entry for YONO DAN to help search engines discover and index the site.",
  "frontend/src/pages/Payment.tsx": "Payment page with UTR submission flow that now shows instant ₹50 bonus on every deposit, displays a detailed success screen with base amount + bonus breakdown, and calls onSuccess immediately after submission to refresh the wallet balance in the header. The UPI phone number has been removed from the payment step display.",
  "frontend/src/pages/Support.tsx": "Support page with quick-topic buttons, contact form, and FAQ section. The UPI phone number card has been removed; only the Response Time card remains in the contact info area.",
  "frontend/src/pages/Withdrawal.tsx": "New Withdrawal page showing available wallet balance and four amount selection buttons (₹100, ₹200, ₹500, ₹1000) with gold highlight on selection, using the existing dark navy/gold theme.",
  "frontend/src/pages/Account.tsx": "Account page updated to accept a navigateTo prop and display a \"Withdraw\" button below the wallet balance that navigates to the withdrawal page."
}